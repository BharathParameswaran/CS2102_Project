<div id="bookingDetails">
    <div class="col-md-12">
        <div class="row">
            <div id="bookingDisplayName" class="col-md-3">
                <h2></h2>
            </div>

            <div id="bookingLogout" class="col-md-2 col-md-offset-10">
                <a>Sign In</a>
                <a>Log Out</a>
            </div>
        </div>

        <div id="detailsReturned" class="col-md-5">
           
        </div>

        <div class="row">
            <div class="col-md-4">
                <button id="cancelBooking" class="btn btn-primary">Cancel Booking</button>
                <button id="makeBooking" class="btn btn-primary">Confirm Booking</button>
            </div>
        </div>
    </div>

</div>